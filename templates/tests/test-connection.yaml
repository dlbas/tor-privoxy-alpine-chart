apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tor-privoxy-alpine-chart.fullname" . }}-test-connection"
  labels:
{{ include "tor-privoxy-alpine-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tor-privoxy-alpine-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
